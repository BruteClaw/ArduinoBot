//Setting up Server, User Name, Password and Channel to Join
String server = "<server>"; //Set the server to connect to, also see below
char serverC[] = "<server>"; //Set up a char for some commands that need it instead of a string
int port = 6667; //Set the port number to use, Default is 6667
String pass = "<password>";  //Set password on servers that require it.  oauth token if you are connecting to twitch
String user = "<bot name>";  //Set the user name
String nick = "<bot name>";  //Set the nickanme
String channel = ""; //Set an empty string for channel switching
String channel1 = "<channel 1>"; //First channel you want to join here
String channel2 = "<channel 2>"; //Second channel you want to join here
